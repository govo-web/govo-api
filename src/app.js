const express=require("express");const cors=require("cors");const app=express();app.use(cors());app.use(express.json());app.get("/health",(req,res)=>res.json({ok:true,service:"govo-api"}));app.use("/api/merchants",require("./modules/merchants"));app.use("/api/riders",require("./modules/riders"));app.use("/api/admin",require("./modules/admin"));app.use("/api/orders",require("./modules/orders"));app.use("/api/meta",require("./modules/meta"));module.exports=app;
